<UserControl x:Class="FleetManagement.WPF.Views.VehiclesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FleetManagement.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,0,10,0" Background="#F9F9F9">
            <StackPanel>
                <TextBlock Text="Registracija vozila" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <TextBlock Text="Marka:" Margin="0,5,0,2"/>
                <TextBox Text="{Binding Make, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalContentAlignment="Center"/>

                <TextBlock Text="Model:" Margin="0,10,0,2"/>
                <TextBox Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalContentAlignment="Center"/>

                <TextBlock Text="Registracija (npr. BG-123-XX):" Margin="0,10,0,2"/>
                <TextBox Text="{Binding PlateNumber, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalContentAlignment="Center"/>

                <TextBlock Text="Početna kilometraža:" Margin="0,10,0,2"/>
                <TextBox Text="{Binding InitialMileage, UpdateSourceTrigger=PropertyChanged}" Height="25" VerticalContentAlignment="Center"/>

                <Button Content="Sačuvaj Vozilo" Command="{Binding RegisterVehicleCommand}" 
                        Height="35" Margin="0,20,0,0" Background="#007ACC" Foreground="White" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Lista vozila u voznom parku" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

            <DataGrid Grid.Row="1" ItemsSource="{Binding VehiclesList}" AutoGenerateColumns="False" 
                      IsReadOnly="True" GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#CCCCCC" Background="White">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Marka" Binding="{Binding Make}" Width="*"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                    <DataGridTextColumn Header="Registracija" Binding="{Binding PlateNumber}" Width="120"/>
                    <DataGridTextColumn Header="Kilometraža" Binding="{Binding CurrentMileage}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
