<UserControl x:Class="FleetManagement.WPF.Views.NotificationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FleetManagement.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sistemska obaveštenja i logovi" FontSize="20" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock Grid.Row="1" Text="Pregled automatskih obaveštenja generisanih nakon završetka vožnji." FontSize="13" Foreground="#666666" Margin="0,0,0,15"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding NotificationsList}" SelectedItem="{Binding SelectedNotification}" AutoGenerateColumns="False" 
                      IsReadOnly="True" GridLinesVisibility="Horizontal" BorderThickness="1" BorderBrush="#CCCCCC" Background="White" Margin="0,0,15,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Vreme" Binding="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm:ss}'}" Width="140"/>
                    <DataGridTextColumn Header="Poruka obaveštenja" Binding="{Binding Message}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Pročitano" Binding="{Binding IsRead, Mode=OneWay}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <Button Content="Osveži listu" Command="{Binding RefreshCommand}" 
                        Height="35" Margin="0,0,0,10" Background="#17A2B8" Foreground="White" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Označi kao pročitano" Command="{Binding MarkAsReadCommand}" 
                        Height="35" Background="#28A745" Foreground="White" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
